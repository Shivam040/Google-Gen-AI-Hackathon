# voice_service.py

# Purpose: STT/TTS & scripted flows (no cloning).
# Do: record artisan narration, generate TTS in local dialect where supported, attach audio URLs to story/product.
# Note: respect policy; prefer stock voices or custom voice profiles if available.